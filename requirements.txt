# Core
python-dotenv>=1.0.0
pydantic>=2.0,<3.0
pydantic-settings>=2.2.0

# Data / numerics
pandas>=2.2.0,<3.0
pyarrow>=15.0.0
numpy>=1.26.0
scipy>=1.12.0
networkx>=3.0.0
faiss-cpu>=1.8.0
# optional: kuiper test; fallback implemented if absent
# astropy>=6.0.0

# Machine learning
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.3.0
imbalanced-learn>=0.12.0
shap>=0.45.0
joblib>=1.3.0
mlflow>=2.10.0

# Stellar / ingestion
httpx[http2]>=0.27.0
sseclient-py>=1.8
stellar-sdk>=10.0,<11.0
packaging>=23.0

# EVM cross-chain detection
web3>=6.0.0
requests>=2.31.0

# Webhook / security
cryptography>=42.0.0

# Zero-knowledge proofs (BN254 elliptic curve)
py_ecc>=7.0.0

# Local API + CLI
tomli>=2.0.0;python_version<"3.11"
fastapi>=0.110.0
strawberry-graphql[fastapi]>=0.235.0
slowapi>=0.1.9
uvicorn[standard]>=0.29.0
typer>=0.12.0
jinja2>=3.1.0
rich>=13.0.0
grpcio>=1.60.0
grpcio-tools>=1.60.0
strawberry-graphql>=0.243.0
# Pin explicitly: generated/scoring_pb2.py's gencode version must not be newer
# than this runtime (regenerate via the command in generated/__init__.py if
# proto/ledgerlens/v1/scoring.proto changes).
protobuf>=6.33.5,<7.0.0

# Distributed rate limiting / caching (detection/feature_store.py hot tier,
# detection/rate_limiter.py per-key rate limits). Both fail open to an
# in-process fallback if unreachable, so this is not a hard runtime
# requirement, but it must be installed for the shared-state paths to
# actually engage rather than silently degrade to per-process-only.
redis>=5.0.0

# Multi-region Soroban submission lease coordination (detection/soroban_lease.py).
# Only required when SOROBAN_SUBMISSION_LEASE_ENABLED=true; imported lazily so
# single-region deployments don't need it installed.
kubernetes>=29.0.0

# Database migrations
alembic>=1.13.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Kubernetes (Soroban submission lease coordination across replicas)
kubernetes>=29.0.0

# Visualization
matplotlib>=3.8.0

# PDF report generation (optional)
# weasyprint>=60.0

# Differential privacy accounting
dp-accounting>=0.4.3

# Causal inference
# dowhy 0.11.x requires Python <3.12; 0.12+ supports 3.12+. CausalModel is
# imported lazily inside function bodies so version bumps are safe at import time.
dowhy>=0.12

# Observability
structlog>=24.1.0
prometheus-client==0.20.0
opentelemetry-sdk>=1.24.0
opentelemetry-api>=1.24.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.3.0
responses>=0.25.0
mutmut>=3.0.0
hypothesis>=6.100.0
fakeredis[lua]>=2.20.0
